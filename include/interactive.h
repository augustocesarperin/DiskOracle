#pragma once

/**
 * @brief Enters the interactive mode of the application.
 * 
 * This function presents a user-friendly menu to select drives and
 * perform diagnostic actions like surface scans and SMART data retrieval.
 * It serves as the main entry point for the UI-driven experience.
 *
 * @return Returns 0 on successful exit, or a non-zero value on error.
 */
int start_interactive_mode(void); 